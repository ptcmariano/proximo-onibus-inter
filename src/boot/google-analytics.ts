import { defineBoot } from '#q-app/wrappers';
//import Gtag from 'vue-gtag';

export default defineBoot((
  //{ app, router }
) => {
  /*
  if (process.env.NODE_ENV === 'production') {
    app.use(
      router,
      {
        config: { id: 'G-281189846' },
      },
      Gtag
    );
  }
    */
});
